import React from "react";
import ReactDOM from "react-dom/client";
import MessirveApp from "./MessirveApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MessirveApp />
  </React.StrictMode>
);
